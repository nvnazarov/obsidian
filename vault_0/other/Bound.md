
A program is:
- I/O bound - if it would go faster if the I/O subsystem was faster
- CPU bound - if it would go faster if the CPU were faster (i.e. it spends the majority of its time simply using the CPU)