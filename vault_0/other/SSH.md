SSH runs on top of TCP/IP protocol suite (just like HTTP, FTP, SMTP).

tunneling

port forwarding

Linux and Mac have SSH client preinstalled (built in), on Windows you may need to install it manually.

default port: 22